@model IEnumerable<Team14_Final_Project.Models.InterviewRoom>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Rooms)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Rooms)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.InterviewRoomID }) |
            @Html.ActionLink("Details", "Details", new { id=item.InterviewRoomID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.InterviewRoomID })
        </td>
    </tr>
}

</table>
